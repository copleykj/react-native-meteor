# Demo

Expo SDK 57 app exercising @socialize/react-native-meteor v4 (hooks, typed Model, optimistic UI).

```sh
node server.mjs   # tiny local DDP server on ws://localhost:3000/websocket
npm start         # then press w (web), a (android), or scan with Expo Go
```
